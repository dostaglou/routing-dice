import React from 'react'

const Options = () => (
  <div>
    <p>Options for erasing history go header</p>
    <p>Maybe also options for setting default roll Mechanics?</p>
  </div>
);

export default Options
